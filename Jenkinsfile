pipeline {
  agent any
  stages {
    stage(build) {
      steps {
        sh "echo testing apps"
        //sh 'npm install'            // java based apps
        //sh 'npm build'
       
        }
    }
    stage(test) {
      steps {
        sh "echo testing apps"
        }
    }
    stage(deploy) {
      steps {
        sh "echo deploying apps"
        }
    }    
  }  
}
